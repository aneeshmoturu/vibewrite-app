generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Generation {
  id        String   @id @default(uuid())
  userId    String   
  prompt    String   
  content   String   
  createdAt DateTime @default(now())
}